# 🎂 Compleanno Alice

Sito regalo per il compleanno di Alice. Login → Countdown → Scelta città → Itinerario.

## Credenziali
- **ID:** `Amore`
- **Password:** `Alice23`

## Struttura (non modificare)
```
alice/
├── public/
│   └── index.html    ← tutto qui dentro
├── server.js
├── package.json
├── .gitignore
└── README.md
```

## Deploy su GitHub + Render

### 1. GitHub
```bash
git init
git add .
git commit -m "first commit"
git remote add origin https://github.com/USERNAME/alice-23.git
git push -u origin main
```

### 2. Render
- New Web Service → connetti repo
- **Build Command:** `npm install`
- **Start Command:** `npm start`
- Deploy
